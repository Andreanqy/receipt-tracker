# receipt-tracker
Receipt Tracker – a backend service that automatically tracks your expenses by scanning receipt QR codes. Upload a photo, and the service extracts the QR code, fetches full purchase details (items, prices, store name, date, total) from the proverkacheka.com API, and stores everything in PostgreSQL for analytics.
